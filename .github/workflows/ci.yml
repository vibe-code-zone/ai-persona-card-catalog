# Generated with Claude Code (claude-sonnet-4) via collaborative "vibe-coding" sessions
name: CI

on:
  pull_request:
    branches: [ main ]
  push:
    branches: [ main ]

jobs:
  python-quality:
    runs-on: ubuntu-latest
    name: Python Code Quality
    
    steps:
    - uses: actions/checkout@v4
    
    - name: Set up Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.11'
    
    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install flake8 black isort
        # Install project dependencies if requirements.txt exists
        if [ -f requirements.txt ]; then pip install -r requirements.txt; fi
    
    - name: Run Black (code formatting check)
      run: black --check --diff .
    
    - name: Run isort (import sorting check)
      run: isort --check-only --diff .
    
    - name: Run flake8 (linting)
      run: flake8 . --count --select=E9,F63,F7,F82 --show-source --statistics

  # markdown-quality:
  #   runs-on: ubuntu-latest
  #   name: Markdown Quality
    
  #   steps:
  #   - uses: actions/checkout@v4
    
  #   - name: Setup Node.js
  #     uses: actions/setup-node@v4
  #     with:
  #       node-version: '18'
    
  #   - name: Install markdown tools
  #     run: |
  #       npm install -g markdownlint-cli
  #       npm install -g markdown-link-check
    
  #   - name: Run markdownlint
  #     run: markdownlint "**/*.md" --ignore node_modules --ignore "**/*_Prompts.md" --ignore "**/prompt_archive_*.md"
    
    # - name: Check markdown links
    #   run: |
    #     find . -name "*.md" -not -path "./node_modules/*" -not -name "*_Prompts.md" -not -name "prompt_archive_*.md" | xargs -I {} markdown-link-check {}
